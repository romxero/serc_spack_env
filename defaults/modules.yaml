modules:
  enable::
    - lmod
  lmod:
    core_compilers:
      - 'gcc@4.8.5'
    hierarchy:
      - 'mpi'
      - 'compiler'
    hash_length: 0
    projections:
      all:          '{name}/{version}'
      ^mpi:      '{name}/{^mpi.name}-{version}'
